# Карта системы: Keenetic ≠ MagiTrickle ≠ Mihomo ≠ MetaCubeX

Эта статья — карта всего, что стоит за Keenetic Auto Setup. Прочитайте её один раз, и
остальные материалы проекта лягут на понятную схему, а не в кучу незнакомых слов.

Главный тезис, который снимает бо́льшую часть путаницы:

> **Keenetic ≠ MagiTrickle ≠ Mihomo ≠ MetaCubeX.**
> Это четыре разные вещи с разными задачами. Ни одна из них не является «другим
> названием» какой-либо из остальных.

---

## Общая схема

```text
Клиент (телефон, ноутбук, телевизор)
   │
   │  DNS-запрос: «на каком адресе youtube.com?»
   ▼
Keenetic DNS ───► MagiTrickle: решает по домену, куда пойдёт трафик
   │
   │  решение (исполняет policy-роутинг Keenetic)
   ├── DIRECT ─────────────────────► провайдер ──► Интернет
   │
   ├── Proxy0 (SOCKS5, 127.0.0.1:7890) ──┐
   │                                     │
   └── mitun0 (TUN-интерфейс Mihomo) ────┤
                                         ▼
                                      Mihomo
                                         │
                                         │  proxies / proxy-groups / rules:
                                         │  «какой сервер для этого соединения?»
                                         ▼
                                      Интернет
```

Параллельно трафику, отдельно от него, живёт панель управления:

```text
MetaCubeX (страница в браузере)
      │  HTTP-запросы
      ▼
external-controller (REST API Mihomo, порт 9090 — если включён)
      │
      ▼
Mihomo
```

Панель **трафик не переносит**. Она только смотрит на Mihomo и щёлкает
переключателями через его API.

---

## Кто есть кто

### Keenetic — платформа

**Что делает:** маршрутизирует трафик домашней сети; предоставляет интерфейсы и
сегменты, политики подключений (policy routing), firewall и DNS-прокси. Установщик
проекта включает в Keenetic перехват транзитного DNS, чтобы запросы клиентов попадали
туда, где их видит MagiTrickle.

**Чем не является:** не «просто коробка с Wi-Fi», но и не прокси — сам Keenetic ничего
не шифрует и не подменяет выход.

**Где заканчивается ответственность:** когда трафик отдан выбранному интерфейсу-назначению
(провайдер, Proxy0, mitun0, VPN-интерфейс). Что происходит дальше — не его уровень.

### MagiTrickle — уровень решений

**Что делает:** читает DNS-запросы, сопоставляет домены с вашими правилами и решает,
через какой интерфейс должен пойти трафик к каждому домену: напрямую (DIRECT), в
Proxy0, в mitun0 и т.д.

**Чем не является:** не VPN и не прокси. MagiTrickle ничего не туннелирует и не
шифрует — он только решает; носит трафик не он, а маршрутизация Keenetic.

**Где заканчивается ответственность:** на доменах, которые до него дошли. Если запрос
клиента не прошёл через наблюдаемый MagiTrickle DNS — решения по этому домену не будет.
Именно поэтому установщик включает перехват транзитного DNS.

### Mihomo — движок маршрутизации («роутер внутри роутера»)

**Что делает:** принимает трафик на свои входы — mixed-порт `127.0.0.1:7890` (прокси)
и/или TUN-интерфейс (`mitun0`) — и по своему `config.yaml` решает, через какой сервер
или выход отправить каждое соединение: десятки серверов, подписки, группы, правила.

**Чем не является:** не «тот самый VPN на роутере» и не часть Keenetic. Для Keenetic
весь Mihomo выглядит как один локальный SOCKS5-прокси на `127.0.0.1:7890` (интерфейс
Proxy0) и/или как сетевой интерфейс `mitun0`.

**Где заканчивается ответственность:** на выходе к своим серверам. Какой трафик вообще
попадёт в Mihomo — решают уровни выше (Keenetic и MagiTrickle), а не он.

### MetaCubeX — экосистема, а чаще всего — панель

**Что делает:** MetaCubeX — это организация и экосистема вокруг Mihomo: само ядро
живёт в `MetaCubeX/mihomo`, официальная веб-панель называется **metacubexd**, там же —
гео-базы для правил. В разговорах «MetaCubeX» почти всегда означают именно веб-панель.

**Чем не является:** **MetaCubeX (панель) — это не сам Mihomo.** Панель — отдельная
страница/программа, которая подключается к Mihomo через его API. Удалите панель —
Mihomo продолжит маршрутизировать трафик, как ни в чём не бывало.

### controller и web UI — два разных «дополнения» к ядру

**controller** — это настройка `external-controller` в `config.yaml`: она включает
REST API Mihomo (обычно на порту 9090). Через этот API панель получает статистику и
управляет ядром.

**web UI** (dashboard, панель) — страница в браузере, которая к этому API подключается.

Ключевые факты (первый — из конфига проекта, второй — из кода Mihomo):

- **По умолчанию в этом проекте controller выключен**: в разворачиваемом конфиге
  строки `external-controller`, `secret` и `external-ui` закомментированы. Не включён
  controller → не работает и API → порт 9090 никто не слушает → панель подключиться не
  может. Это состояние — норма, а не поломка.
- **«Дефолтного» адреса 127.0.0.1:9090 у контроллера нет** — это конвенция из примеров.
  Подробнее: [13-127-0-0-1-i-ip-routera.md](13-127-0-0-1-i-ip-routera.md).

---

## Почему это важно для новичка

- Mihomo **работает без всякой панели** — установка по умолчанию ставит ровно демон.
- Панель подключается **не к «сайту Mihomo», а к API**, которое нужно сначала включить.
- Адрес `127.0.0.1` в поле панели означает «этот компьютер», а не роутер — самая
  частая точка отказа. Разбор: [12-pervyj-vhod-v-ui.md](12-pervyj-vhod-v-ui.md) и
  [13-127-0-0-1-i-ip-routera.md](13-127-0-0-1-i-ip-routera.md).

---

## Куда идти дальше

- [01-slovar.md](01-slovar.md) — минимальный словарь новичка.
- [10-mihomo-eto.md](10-mihomo-eto.md) — что такое Mihomo и где он в системе.
- [11-metacubex-eto.md](11-metacubex-eto.md) — что такое MetaCubeX и зачем он рядом с Mihomo.
- [12-pervyj-vhod-v-ui.md](12-pervyj-vhod-v-ui.md) — включить панель и подключить её.
- [13-127-0-0-1-i-ip-routera.md](13-127-0-0-1-i-ip-routera.md) — про 127.0.0.1 и IP роутера.
- [../../ARCHITECTURE.md](../../ARCHITECTURE.md) — полная архитектура: три пути трафика,
  ProxyN vs mitun0, DNS-перехват, границы проекта.
- [../HOWTO_RU.md](../HOWTO_RU.md) — полное практическое руководство.
